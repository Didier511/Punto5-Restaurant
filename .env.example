# Public client settings only. Values prefixed with VITE_ are bundled into the browser.
VITE_APP_NAME="Punto 5"
VITE_API_BASE_URL="http://localhost:4000/api"

# Keep private values in the backend environment, never in the React app:
# MONGODB_URI=
# JWT_SECRET=
# SESSION_SECRET=
# CLOUDINARY_API_SECRET=
